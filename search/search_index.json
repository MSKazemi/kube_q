{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Home","text":"<p>v1.4.0 \u00b7 stable release</p> <p></p> 0 minutes to first query 0 slash commands 0% local session history 0 kubectl flags to memorise <p>Why kube-q</p>"},{"location":"#talk-to-kubernetes-in-plain-english","title":"Talk to Kubernetes. In plain English.","text":"<p><code>kube-q</code> is an AI-native terminal client for Kubernetes. Query, debug, and operate any cluster the way you think \u2014 with streaming answers, session memory, human-in-the-loop approvals, and a full browser REPL.</p> <p>Get Started \u00a0  View on GitHub \u00a0 </p> :material-tag: v1.4.0 :material-language-python: Python 3.12+ :material-scale-balance: MIT License :material-lightning-bolt: Streaming SSE :material-docker: Docker Ready :material-database: SQLite History :material-shield-check: HITL Safe"},{"location":"#a-kubernetes-client-that-understands-you","title":"A Kubernetes client that understands you","text":"<p>Stop context-switching between docs, cheat-sheets and your terminal. Ask a question, get an answer \u2014 with the evidence right there.</p> <ul> <li> <p> Plain-English Queries</p> <p>No more kubectl reference card. Ask \"why are my pods crashing in production?\" and get a structured, expert-level answer in seconds.</p> <p> CLI Reference</p> </li> <li> <p> Persistent Session Memory</p> <p>Every conversation is saved locally in SQLite. Resume any past session, search across all history with full-text FTS5 queries, branch at any turn.</p> <p> Session History</p> </li> <li> <p> Human-in-the-Loop Safety</p> <p>Before a destructive action runs, <code>kube-q</code> pauses and shows you the exact command, risk level and diff. You approve \u2014 or deny.</p> <p> HITL Guide</p> </li> <li> <p> File Attachments</p> <p>Embed YAML manifests, logs, configs and more with <code>@filename</code>. The AI sees full file contents inline alongside your question.</p> <p> File Attachments</p> </li> <li> <p> Browser Terminal</p> <p>The full <code>kq</code> REPL in any browser via Docker \u2014 xterm.js, WebSocket, node-pty. No duplicated logic. Every command, every stream, identical.</p> <p> Web UI</p> </li> <li> <p> Token &amp; Cost Tracking</p> <p>Every response shows token count and elapsed time. <code>/tokens</code> gives a full breakdown with estimated dollar cost, configurable per model.</p> <p> Token Tracking</p> </li> <li> <p> Multi-Backend &amp; Multi-Cluster</p> <p>One CLI, three backends: kube-q server, direct OpenAI, or Azure OpenAI \u2014 pick with <code>--backend</code>. Switch kubectl context live via <code>/context</code>, bundle cluster + keys into named profiles, add custom slash commands with plugins.</p> <p> Configuration</p> </li> </ul> <p>Install</p>"},{"location":"#up-and-running-in-under-a-minute","title":"Up and running in under a minute","text":"<p>Pick your flavour. One command and you're in.</p>  pipx (recommended) pip Homebrew Docker <pre><code>pipx install kube-q\nkq\n</code></pre> <pre><code>pip install kube-q\nkq\n</code></pre> <pre><code>brew tap MSKazemi/kube-q\nbrew install kube-q\nkq\n</code></pre> <pre><code>docker run -p 3000:3000 \\\n  -e KUBE_Q_URL=https://kube-q.example.com \\\n  -e KUBE_Q_API_KEY=your-key \\\n  ghcr.io/mskazemi/kube_q:latest\n</code></pre> <p>Open <code>http://localhost:3000</code> for a full REPL in your browser.</p> <p>Point at your backend</p> <p><pre><code>kq --url https://kube-q.example.com --api-key your-key\n</code></pre> Or set it once in <code>~/.kube-q/.env</code> and never type it again. See Configuration.</p> <p>Live demo</p>"},{"location":"#real-conversation-real-cluster","title":"Real conversation, real cluster","text":"<p>This is a verbatim kube-q session \u2014 from a vague question to a safe, approved fix.</p> kq \u2014 kube-q REPL  \u00b7  ns: production <pre><code>kube-q  v1.4.0  |  session abc123  |  ns: production\n\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\nYou&gt; why are the pods in the payment service crashlooping?\n\nkube-q&gt; Investigating... \u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501  (3 tool calls)\n\n  The payment-service pods are in CrashLoopBackOff due to a missing\n  environment variable: DATABASE_URL is not set in the deployment spec.\n\n  The container exits immediately with:\n    Error: required env DATABASE_URL is not set\n\n  Fix:\n  \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n  \u2502 kubectl set env deployment/payment-service \\                \u2502\n  \u2502   DATABASE_URL=postgres://db.prod.svc:5432/payments         \u2502\n  \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n\n  (1.8s \u00b7 820 tokens)\n\nYou&gt; apply the fix\n\nkube-q&gt; \u256d\u2500 Action requires approval \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u256e\n        \u2502  kubectl set env deployment/payment-service \\       \u2502\n        \u2502    DATABASE_URL=postgres://db.prod.svc:5432/        \u2502\n        \u2502                                                     \u2502\n        \u2502  Risk: medium  \u00b7  namespace: production             \u2502\n        \u2502  Type /approve to proceed or /deny to cancel.       \u2502\n        \u2570\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u256f\nHITL&gt; /approve\n\nkube-q&gt; Done. Rollout triggered. Monitoring...\n        All 3 pods are Running (45s).\n</code></pre> <p>Comparison</p>"},{"location":"#kube-q-vs-raw-kubectl","title":"kube-q vs. raw kubectl","text":"<p>Both have their place \u2014 kube-q is built for the moments when you'd rather think out loud than recite syntax.</p> Capability raw kubectl kube-q Natural-language queries \u2717 \u2713 streaming Multi-resource root-cause analysis manual (logs + events + describe) \u2713 automatic Persistent session history shell history only \u2713 SQLite + FTS5 search Conversation branching \u2717 \u2713 <code>/branch</code> Human-in-the-loop approvals \u2717 \u2713 for destructive ops Token &amp; cost accounting N/A \u2713 per-response + session Browser REPL \u2717 \u2713 Docker image Python SDK client-go \u2713 sync &amp; async <p>How it works</p>"},{"location":"#architecture-at-a-glance","title":"Architecture at a glance","text":"<p>Everything runs through a streaming backend. Your cluster credentials never leave the server.</p> <pre><code>sequenceDiagram\n    participant U as You\n    participant K as kube-q CLI\n    participant B as kube-q Backend\n    participant C as Kubernetes Cluster\n\n    U-&gt;&gt;K: \"why are my pods crashing in production?\"\n    K-&gt;&gt;B: POST /chat (streaming SSE)\n    B-&gt;&gt;C: kubectl get pods, logs, events\n    C--&gt;&gt;B: pod state, log tail, events\n    B--&gt;&gt;K: token stream (real-time)\n    K--&gt;&gt;U: rendered markdown answer\n    note over K,U: HITL pause if destructive action detected</code></pre> <p>Built for</p>"},{"location":"#use-cases-weve-heard","title":"Use cases we've heard","text":"<p>From the first-time learner to the 3 AM on-call responder.</p> <p>What's included</p>"},{"location":"#incident-response","title":"Incident response","text":"<p>First signal of trouble, you ask <code>kube-q</code> what changed. It correlates events, logs and recent rollouts so you stop hunting and start deciding.</p>"},{"location":"#learning-k8s","title":"Learning k8s","text":"<p>A junior engineer can ask \"what does a readiness probe do for this pod?\" and get an answer grounded in the actual manifest \u2014 not a blog post.</p>"},{"location":"#safer-rollouts","title":"Safer rollouts","text":"<p>Destructive actions pause for an explicit <code>/approve</code>. Risk level and blast-radius are surfaced before anything touches the cluster.</p>"},{"location":"#forensic-search","title":"Forensic search","text":"<p><code>/search \"oom killed\"</code> walks every past session. Every investigation becomes institutional memory for the next one.</p>"},{"location":"#scenario-exploration","title":"Scenario exploration","text":"<p><code>/branch</code> forks a conversation the moment you want to try a different fix \u2014 without losing the original line of reasoning.</p>"},{"location":"#automation-bots","title":"Automation &amp; bots","text":"<p>Drive <code>kube-q</code> from a script or chatbot via the Python SDK. The same streaming, same HITL, same audit trail.</p>"},{"location":"#one-project-three-surfaces","title":"One project. Three surfaces.","text":"<p>Pick the interface that fits the moment \u2014 they all share the same backend and session store.</p> <ul> <li> <p> Terminal Client</p> <p>The <code>kq</code> command \u2014 a feature-rich interactive REPL built on <code>prompt_toolkit</code> with Tab-completion, command history, multi-line input, rich markdown rendering, and a full set of slash commands.</p> </li> <li> <p> Python SDK</p> <p>Use <code>KubeQClient</code> (sync) or <code>AsyncKubeQClient</code> directly in scripts, notebooks, or your own tools \u2014 no CLI required. Full streaming and event-model support.</p> <p> SDK reference</p> </li> <li> <p> Browser Terminal</p> <p>A Docker image that runs the full <code>kq</code> REPL in any browser via xterm.js + WebSocket + node-pty. Embeddable in iframes. Each tab is an isolated process.</p> <p> Web UI</p> </li> </ul> <p>kube-q turns the terminal into a conversation. The things I used to copy-paste from Stack Overflow \u2014 <code>describe</code>, <code>get events</code>, <code>logs --previous</code>, <code>top pods</code> \u2014 all just happen when I ask a question. It's the shortest path from \"something's wrong\" to \"here's what and here's why.\" \u2014 The tool the author wishes they'd had on every 3 AM page.</p>"},{"location":"#ready-to-try-it","title":"Ready to try it?","text":"<p>Install in one command. Point at your backend. Start asking questions.</p> <p>Quick Start \u00a0  Star on GitHub \u00a0 </p>"},{"location":"404/","title":"Page not found","text":"<p>Error 404</p> <ul> <li> <p> Quick Start</p> <p>Install <code>kq</code> and send your first query in under five minutes.</p> <p> Quick Start</p> </li> <li> <p> CLI Reference</p> <p>Every flag, every command, every argument.</p> <p> CLI Reference</p> </li> <li> <p> FAQ</p> <p>Answers to the questions we get most often.</p> <p> FAQ</p> </li> <li> <p> Troubleshooting</p> <p>Connection issues, TLS errors, stale state \u2014 start here.</p> <p> Troubleshooting</p> </li> </ul>"},{"location":"404/#that-page-took-a-wrong-turn","title":"That page took a wrong turn.","text":"<p>The link you followed may be broken, or the page may have moved. Let's get you somewhere useful.</p> <p>Go Home \u00a0  Quick Start \u00a0 </p>"},{"location":"changelog/","title":"Changelog","text":"<p>All notable changes to kube-q are documented here.</p>"},{"location":"changelog/#140-2026-04-14","title":"[1.4.0] \u2014 2026-04-14","text":""},{"location":"changelog/#added-web-ui-docker","title":"Added \u2014 Web UI (Docker)","text":"<ul> <li>Browser terminal \u2014 full kube-q REPL accessible in any browser via xterm.js + WebSocket \u2192 node-pty \u2192 <code>kq</code>; no client-side logic duplicated, all commands and streaming stay in the Python process</li> <li><code>web/server.mjs</code> \u2014 production single-port server: Next.js HTTP + PTY WebSocket (<code>/pty-ws</code>) on one port; spawns a <code>kq</code> process per connection</li> <li><code>web/pty-server.mjs</code> \u2014 dev standalone PTY WebSocket server on port 3001; run alongside <code>next dev</code> via <code>npm run dev</code></li> <li><code>Dockerfile</code> \u2014 multi-stage build: Node builder compiles Next.js, runtime stage installs <code>kube-q</code> from PyPI + copies built app; env vars injected at runtime (<code>KUBE_Q_URL</code>, <code>KUBE_Q_API_KEY</code>)</li> <li>iframe / basePath support \u2014 <code>NEXT_PUBLIC_BASE_PATH</code> env var relocates the Next.js app to a sub-path; <code>Content-Security-Policy: frame-ancestors *</code> and <code>X-Frame-Options: ALLOWALL</code> headers allow embedding in any parent page</li> <li>Download conversation button \u2014 toolbar \"\u2b07 Download\" button exports the xterm scrollback buffer as a <code>.md</code> file directly to the browser</li> <li>Custom branding \u2014 <code>KUBE_Q_LOGO</code> sets a custom ASCII banner logo; <code>KUBE_Q_TAGLINE</code> sets a custom copyright / tagline line; both configurable via <code>.env</code> or environment variable</li> </ul>"},{"location":"changelog/#added-session-search-branching","title":"Added \u2014 Session Search &amp; Branching","text":"<ul> <li><code>kq --search &lt;query&gt;</code> / <code>/search &lt;query&gt;</code> \u2014 FTS5 full-text search across all session history with highlighted match snippets; supports FTS5 boolean syntax (<code>pods AND NOT staging</code>); old databases backfilled during migration</li> <li><code>/branch</code> \u2014 fork the current conversation at the current message count into a new independent session; original is preserved; <code>/branches</code> lists all forks; <code>/title &lt;text&gt;</code> renames a session</li> <li>SQLite schema v3 \u2014 <code>messages_fts</code> FTS5 virtual table with insert/delete triggers, <code>parent_session_id</code> and <code>branch_point</code> columns; branches are ordinary sessions so search finds them automatically</li> </ul>"},{"location":"changelog/#added-token-cost-tracking","title":"Added \u2014 Token &amp; Cost Tracking","text":"<ul> <li>Token footer \u2014 every response shows <code>(1.2s \u00b7 460 tokens)</code> when the server emits a <code>usage</code> block; servers that omit <code>usage</code> behave exactly as before</li> <li><code>/tokens</code> / <code>/cost</code> \u2014 Rich panel with per-session prompt/completion/total counts, request count, and estimated dollar cost; rates overridable via <code>KUBE_Q_COST_PER_1K_PROMPT</code> / <code>KUBE_Q_COST_PER_1K_COMPLETION</code></li> <li><code>kq --list</code> tokens column \u2014 session listing shows total token count per session; SQLite schema auto-migrates from v1</li> </ul>"},{"location":"changelog/#added","title":"Added","text":"<ul> <li>Configurable display names \u2014 <code>--user-name</code> / <code>--agent-name</code> CLI flags, <code>KUBE_Q_USER_NAME</code> / <code>KUBE_Q_AGENT_NAME</code> env vars</li> <li>Friendly HTTP 401 error handling \u2014 clear actionable message instead of raw JSON</li> <li><code>.env</code> file support \u2014 all settings configurable via <code>KUBE_Q_*</code> env vars; <code>~/.kube-q/.env</code> and <code>./.env</code> loaded automatically</li> <li>Removed YAML config file (<code>pyyaml</code> dependency removed)</li> <li>Renamed config directory to <code>~/.kube-q/</code></li> </ul>"},{"location":"changelog/#100-2026-04-10","title":"[1.0.0] \u2014 2026-04-10","text":""},{"location":"changelog/#added_1","title":"Added","text":"<ul> <li>Interactive REPL with streaming SSE responses</li> <li>Human-in-the-Loop (HITL) approve/deny flow</li> <li>Persistent conversation history and user ID</li> <li>Namespace context switching (<code>/ns</code>)</li> <li>Conversation save to markdown (<code>/save</code>)</li> <li>Built-in demo scenarios (deploy, debug, hitl, security, scale)</li> <li>Single-query mode (<code>--query</code>)</li> <li>Health check with retry on startup</li> <li>Multi-line copy-paste support via <code>prompt_toolkit</code></li> <li>Rich syntax highlighting for YAML/JSON code blocks</li> <li>File attachment support via <code>@filename</code> in messages</li> <li><code>--api-key</code> / <code>KUBE_Q_API_KEY</code> authentication</li> <li><code>--ca-cert</code> for corporate proxies / self-signed certificates</li> <li><code>--output plain</code> flag for pipe-friendly output</li> <li>Live token streaming</li> <li>Typo suggestions for unknown slash commands</li> </ul>"},{"location":"cli-reference/","title":"CLI Reference","text":"<pre><code>kq [options]\n</code></pre>"},{"location":"cli-reference/#flags","title":"Flags","text":"Flag Default Description <code>--url URL</code> <code>https://api.kubeintellect.com</code> kube-q API base URL (env: <code>KUBE_Q_URL</code>) <code>--query TEXT</code> / <code>-q TEXT</code> \u2014 Run a single query and exit (non-interactive) <code>--no-stream</code> off Disable streaming \u2014 wait for full response <code>--session-id ID</code> \u2014 Resume a previous session by ID \u2014 loads history from the local store and replays the stored transcript on launch (user turns in green, assistant turns as markdown). For interactive picking use <code>/sessions</code> or <code>/resume</code> inside the REPL. <code>--list</code> \u2014 List recent sessions and exit <code>--search QUERY</code> \u2014 Full-text search across session history and exit <code>--user-id ID</code> auto Persistent user ID (saved to <code>~/.kube-q/user-id</code>) <code>--api-key KEY</code> \u2014 Bearer token for auth-enabled servers (env: <code>KUBE_Q_API_KEY</code>) <code>--ca-cert PATH</code> \u2014 Custom CA certificate bundle for TLS verification <code>--output {rich,plain}</code> <code>rich</code> <code>rich</code> for markdown rendering; <code>plain</code> for pipe-friendly raw text <code>--model NAME</code> <code>kubeintellect-v2</code> Model name sent in requests (env: <code>KUBE_Q_MODEL</code>) <code>--user-name NAME</code> <code>You</code> Your display name in the prompt (env: <code>KUBE_Q_USER_NAME</code>) <code>--agent-name NAME</code> <code>kube-q</code> Assistant name in saved conversations (env: <code>KUBE_Q_AGENT_NAME</code>) <code>--no-banner</code> / <code>--quiet</code> off Suppress logo and header (useful for screen recordings) <code>--no-health-check</code> off Skip startup health-check retry loop (env: <code>KUBE_Q_SKIP_HEALTH_CHECK</code>) <code>--debug</code> / <code>--verbose</code> off Log raw HTTP requests/responses to stderr and <code>~/.kube-q/kube-q.log</code> <code>--version</code> \u2014 Print version and exit"},{"location":"cli-reference/#backend-selection","title":"Backend selection","text":"Flag Default Description <code>--backend {kube-q,openai,azure}</code> <code>kube-q</code> Select the LLM backend (env: <code>KUBE_Q_BACKEND</code>) <code>--openai-api-key KEY</code> \u2014 OpenAI API key when <code>--backend openai</code> (env: <code>KUBE_Q_OPENAI_API_KEY</code>) <code>--openai-endpoint URL</code> <code>https://api.openai.com</code> Override OpenAI endpoint (env: <code>KUBE_Q_OPENAI_ENDPOINT</code>) <code>--azure-openai-api-key KEY</code> \u2014 Azure OpenAI API key (env: <code>KUBE_Q_AZURE_OPENAI_API_KEY</code>) <code>--azure-openai-endpoint URL</code> \u2014 Azure OpenAI resource URL (env: <code>KUBE_Q_AZURE_OPENAI_ENDPOINT</code>) <code>--azure-openai-deployment NAME</code> \u2014 Azure OpenAI deployment name (env: <code>KUBE_Q_AZURE_OPENAI_DEPLOYMENT</code>)"},{"location":"cli-reference/#multi-cluster","title":"Multi-cluster","text":"Flag Default Description <code>--profile NAME</code> \u2014 Load <code>~/.kube-q/profiles/&lt;NAME&gt;.env</code> on top of defaults (env: <code>KUBE_Q_PROFILE</code>) <code>--context NAME</code> \u2014 Set active kubectl context at launch (env: <code>KUBE_Q_CONTEXT</code>)"},{"location":"cli-reference/#examples","title":"Examples","text":""},{"location":"cli-reference/#interactive-repl","title":"Interactive REPL","text":"<pre><code># Connect to local backend (default)\nkq\n\n# Connect to remote backend\nkq --url https://kube-q.example.com\n\n# With API key\nkq --url https://kube-q.example.com --api-key my-secret-key\n\n# Custom display names\nkq --user-name Alice --agent-name KubeBot\n\n# Skip the startup health check (faster start when server is known-good)\nkq --no-health-check\n</code></pre>"},{"location":"cli-reference/#backend-selection_1","title":"Backend selection","text":"<pre><code># Direct OpenAI \u2014 bypass the kube-q server entirely\nkq --backend openai --openai-api-key sk-...\n\n# Azure OpenAI \u2014 supply key, endpoint, and deployment name\nkq --backend azure \\\n   --azure-openai-api-key    \"$AZ_KEY\" \\\n   --azure-openai-endpoint   https://my-resource.openai.azure.com \\\n   --azure-openai-deployment gpt-4o\n</code></pre> <p>The selected backend is fixed for the lifetime of the REPL \u2014 re-launch to switch. <code>/state</code> shows the active backend; the plain-HTTP warning is suppressed for <code>openai</code>/<code>azure</code>.</p>"},{"location":"cli-reference/#multi-cluster_1","title":"Multi-cluster","text":"<pre><code># Create a profile that bundles backend + keys + kubectl context\nkq config profile new prod\n# edit ~/.kube-q/profiles/prod.env, then:\nkq --profile prod\n\n# Pick a kubectl context at launch or flip it live inside the REPL\nkq --context prod-cluster\n# \u2026or inside the REPL:\n/context staging-cluster\n/context                       # clear\n</code></pre>"},{"location":"cli-reference/#single-query-mode","title":"Single-query mode","text":"<pre><code># Print response and exit\nkq --query \"show all pods in the default namespace\"\n\n# Pipe-friendly plain text\nkq --query \"list failing deployments\" --output plain\n\n# Combine with jq or other tools\nkq --query \"show pod names\" --output plain | grep \"crash\"\n</code></pre>"},{"location":"cli-reference/#session-management","title":"Session management","text":"<pre><code># List the 20 most recent sessions\nkq --list\n\n# Resume a session by ID (shown in --list output)\nkq --session-id abc123\n\n# Search across all past sessions\nkq --search \"deployment rollback\"\nkq --search \"pods AND crash AND NOT staging\"\n</code></pre>"},{"location":"cli-reference/#debugging","title":"Debugging","text":"<pre><code># Log raw HTTP traffic to stderr + ~/.kube-q/kube-q.log\nkq --debug\n\n# Custom CA cert (corporate proxies, self-signed certs)\nkq --url https://internal.example.com --ca-cert /etc/ssl/my-ca.pem\n</code></pre>"},{"location":"cli-reference/#environment-variables","title":"Environment variables","text":"<p>All flags can be set via environment variables. See Configuration for the full list.</p> <pre><code>export KUBE_Q_URL=https://kube-q.example.com\nexport KUBE_Q_API_KEY=my-key\nkq\n</code></pre>"},{"location":"commands/","title":"In-REPL Commands","text":"<p>All commands start with <code>/</code>. Press <code>Tab</code> after <code>/</code> to see completions with descriptions. Close matches trigger a typo hint (e.g. <code>/hlep</code> \u2192 \"did you mean /help?\").</p>"},{"location":"commands/#connection-config","title":"Connection &amp; config","text":"<p>Configure the backend URL, API key, and other settings without leaving <code>kq</code>. Settings are written to <code>~/.kube-q/.env</code> and take effect immediately in the current session.</p> Command Description <code>/config</code> Print every config key, its value, and where it came from <code>/config set KEY=VALUE</code> Write a key to <code>~/.kube-q/.env</code> (validated, takes effect immediately) <code>/config reset KEY</code> Remove a single key from <code>~/.kube-q/.env</code> <code>/config reset</code> Wipe <code>~/.kube-q/.env</code> entirely <p><code>KEY</code> accepts either the full env-var name (<code>KUBE_Q_URL</code>) or the short alias (<code>url</code>).</p> <p>Examples</p> <pre><code>/config set url=https://kube-q.example.com\n/config set api_key=your-key-here\n/config set model=kubeintellect-v2\n\n/config reset api_key\n/config\n</code></pre>"},{"location":"commands/#conversation","title":"Conversation","text":"Command Description <code>/new</code> Start a fresh conversation \u2014 clears history, generates a new session ID <code>/id</code> Show the current conversation ID <code>/state</code> Show full session state: ID, user, message count, tokens, namespace, HITL flag <code>/title &lt;text&gt;</code> Rename the current session <code>/save</code> Save conversation to a timestamped Markdown file <code>/save &lt;file&gt;</code> Save to a specific file \u2014 Tab completes paths <code>/clear</code> Clear the terminal screen <code>/help</code> Show full in-REPL help <code>/version</code> Print the installed kube-q version <code>/quit</code> / <code>/exit</code> / <code>/q</code> Exit kube-q <p>Examples</p> <pre><code>/title Production incident \u2014 OOM in payment service\n/save ~/reports/incident-2026-04.md\n/state\n/new\n</code></pre>"},{"location":"commands/#namespace","title":"Namespace","text":"Command Description <code>/ns &lt;name&gt;</code> Set active namespace \u2014 prepended to every query automatically <code>/ns</code> Clear the active namespace <p>The active namespace is shown in the prompt. Tab-completes from the cluster (cached after first use).</p> <p>Examples</p> <pre><code>/ns production\n/ns kube-system\n/ns\n</code></pre>"},{"location":"commands/#kubernetes-context","title":"Kubernetes context","text":"Command Description <code>/context &lt;name&gt;</code> Set active kubectl context \u2014 prepended to every query as <code>[context: X]</code> <code>/context</code> Clear the active context <p>Context names Tab-complete from your kubeconfig. Set at launch with <code>--context &lt;name&gt;</code> or <code>KUBE_Q_CONTEXT=&lt;name&gt;</code>.</p> <p>Examples</p> <pre><code>/context prod-cluster\n/context staging-gke\n/context\n</code></pre>"},{"location":"commands/#session-history","title":"Session history","text":"Command Description <code>/sessions</code> Interactive picker of the 20 most recent sessions \u2014 \u2191/\u2193 to navigate, Enter to resume, Esc to cancel <code>/resume</code> Alias for <code>/sessions</code> <code>/list</code> Print recent sessions as a plain table (same data, no interactive picker) <code>/history</code> Replay all messages in the current session <code>/history &lt;N&gt;</code> Show the last N messages <code>/history &lt;X-Y&gt;</code> Show messages X through Y (1-indexed, inclusive) <code>/history #&lt;N&gt;</code> Show just message #N <code>/forget</code> Delete the current session from local history (server-side data untouched) <p>Resuming a session via <code>/sessions</code> also restores the saved kube context. Each replayed message is prefixed with <code>[#N]</code> so you can reference specific messages.</p> <p>Examples</p> <pre><code>/sessions\n/list\n/history 5\n/history 2-8\n/history #3\n/forget\n</code></pre>"},{"location":"commands/#search-branching","title":"Search &amp; branching","text":"Command Description <code>/search &lt;query&gt;</code> Full-text search across all past sessions with highlighted snippets <code>/branch</code> Fork this conversation at the current point into a new independent session <code>/branches</code> List all forks (and siblings) of this session <p>FTS5 boolean syntax is supported:</p> <p>Examples</p> <pre><code>/search \"crash loop\" AND production\n/search pods AND NOT staging\n/search \"oom killed\" OR \"memory limit\"\n/search deployment AND rollback\n\n/branch\n/branches\n</code></pre>"},{"location":"commands/#profiles-plugins","title":"Profiles &amp; plugins","text":"Command Description <code>/profile</code> List profiles in <code>~/.kube-q/profiles/</code> and show which is active <code>/profile new &lt;name&gt;</code> Create a new profile <code>.env</code> from the template <code>/profile show &lt;name&gt;</code> Print a profile's contents <code>/profile delete &lt;name&gt;</code> Delete a profile file <code>/profile &lt;name&gt;</code> Show the restart command to activate a profile <code>/plugins</code> List slash commands registered by plugins in <code>~/.kube-q/plugins/</code> <p>Profiles are <code>.env</code> fragments \u2014 one per cluster or environment. Switching profiles requires a restart. See Configuration for details.</p> <p>Examples</p> <pre><code>/profile\n/profile new staging\n/profile show staging\n/profile delete old-profile\n\n# To switch, restart with:\nkq --profile staging\n</code></pre>"},{"location":"commands/#token-usage","title":"Token usage","text":"Command Description <code>/tokens</code> Show token counts and estimated cost for this session <code>/cost</code> Alias for <code>/tokens</code> <p>Override cost rates via <code>KUBE_Q_COST_PER_1K_PROMPT</code> and <code>KUBE_Q_COST_PER_1K_COMPLETION</code> env vars. See Token Tracking for details.</p> <p>Examples</p> <pre><code>/tokens\n/cost\n</code></pre>"},{"location":"commands/#human-in-the-loop","title":"Human-in-the-Loop","text":"Command Description <code>/approve</code> Approve a pending action \u2014 the AI executes it <code>/deny</code> Deny a pending action \u2014 nothing is applied <p>The prompt changes to <code>HITL&gt;</code> while an action is pending. See Human-in-the-Loop for details.</p> <p>Examples</p> <pre><code>HITL&gt; /approve\nHITL&gt; /deny\n</code></pre>"},{"location":"commands/#keyboard-shortcuts","title":"Keyboard shortcuts","text":"Key Action <code>Enter</code> Send message <code>Alt+Enter</code> or <code>Esc</code> \u2192 <code>Enter</code> Insert newline (multi-line input) <code>Tab</code> Auto-complete slash commands and paths <code>\u2191</code> / <code>\u2193</code> Scroll through input history <code>Ctrl+A</code> Jump to start of line <code>Ctrl+E</code> Jump to end of line <code>Ctrl+W</code> Delete previous word <code>Ctrl+U</code> Clear entire input buffer <code>Ctrl+C</code> Cancel current input (keeps history) <code>Ctrl+D</code> Exit the session"},{"location":"commands/#file-attachments","title":"File attachments","text":"<p>Type <code>@path/to/file</code> anywhere in your message to attach a file. Its contents are embedded as a code block and sent with the message.</p> <pre><code>What's wrong here? @pod.yaml\nCompare these two: @deploy-prod.yaml @deploy-staging.yaml\nCheck my config: @~/configs/service.json\n</code></pre> <p>Supported types: YAML, JSON, Python, Shell, Go, Terraform, text, logs, and more. Limit: 100 KB per file.</p>"},{"location":"commands/#launch-flags-quick-reference","title":"Launch flags (quick reference)","text":"<p>These are CLI flags passed when starting <code>kq</code>, not REPL commands. See Configuration for the full list.</p> <pre><code># Connect to a specific backend\nkq --url https://kube-q.example.com --api-key sk-your-key\n\n# One-shot query, no REPL\nkq --query \"why are payment pods crashlooping?\"\n\n# Resume a previous session\nkq --session-id abc123\n\n# Search past sessions\nkq --search \"oom killed\"\n\n# Use a profile\nkq --profile staging\n\n# Skip the startup health check\nkq --no-health-check\n\n# Plain text output (no markdown rendering)\nkq --output plain\n\n# Show raw HTTP traffic\nkq --debug\n</code></pre>"},{"location":"configuration/","title":"Configuration","text":"<p>kube-q loads configuration from <code>.env</code> files and environment variables. CLI flags override everything.</p>"},{"location":"configuration/#priority-order","title":"Priority order","text":"<pre><code>CLI flag  &gt;  shell env var  &gt;  ./.env  &gt;  ~/.kube-q/.env  &gt;  built-in default\n</code></pre>"},{"location":"configuration/#env-file-locations","title":".env file locations","text":"Location Priority Use case <code>~/.kube-q/.env</code> lower Persistent user-level defaults across all clusters <code>./.env</code> (current directory) higher Per-project or per-cluster overrides <p>Shell-exported variables always win over <code>.env</code> files. You can mix both \u2014 common settings in <code>~/.kube-q/.env</code>, cluster-specific overrides in <code>./.env</code>.</p>"},{"location":"configuration/#all-variables","title":"All variables","text":"<pre><code># \u2500\u2500 Connection \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nKUBE_Q_URL=https://api.kubeintellect.com  # kube-q API base URL\nKUBE_Q_API_KEY=                         # Bearer token (required when server auth is enabled)\nKUBE_Q_MODEL=kubeintellect-v2           # Model name sent in requests\n\n# \u2500\u2500 Timeouts (seconds) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nKUBE_Q_TIMEOUT=120                      # Per-query HTTP timeout\nKUBE_Q_HEALTH_TIMEOUT=5                 # Health-check HTTP timeout\nKUBE_Q_NAMESPACE_TIMEOUT=3             # Namespace fetch timeout\nKUBE_Q_STARTUP_RETRY_TIMEOUT=300       # How long to retry on startup before giving up\nKUBE_Q_STARTUP_RETRY_INTERVAL=5        # Seconds between startup retries\n\n# \u2500\u2500 Output \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nKUBE_Q_STREAM=true                      # Enable/disable streaming responses\nKUBE_Q_OUTPUT=rich                      # rich | plain\nKUBE_Q_LOG_LEVEL=INFO                   # DEBUG | INFO | WARNING | ERROR\nKUBE_Q_SKIP_HEALTH_CHECK=false          # Skip startup health-check loop\n\n# \u2500\u2500 Display names \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nKUBE_Q_USER_NAME=You                    # Your display name in the prompt\nKUBE_Q_AGENT_NAME=kube-q               # Assistant name in saved conversations\n\n# \u2500\u2500 Custom branding \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nKUBE_Q_LOGO=KubeIntellect               # Custom ASCII banner logo (\\\\n for newlines)\nKUBE_Q_TAGLINE=\u00a9 2025 Acme Corp        # Custom tagline / copyright line\n\n# \u2500\u2500 Token cost overrides \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nKUBE_Q_COST_PER_1K_PROMPT=0.003        # Override prompt token cost rate\nKUBE_Q_COST_PER_1K_COMPLETION=0.006    # Override completion token cost rate\n\n# \u2500\u2500 Backend selection \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nKUBE_Q_BACKEND=kube-q                  # kube-q | openai | azure\nKUBE_Q_OPENAI_API_KEY=                 # used when backend=openai\nKUBE_Q_OPENAI_ENDPOINT=https://api.openai.com\nKUBE_Q_OPENAI_MODEL=gpt-4o-mini\nKUBE_Q_AZURE_OPENAI_API_KEY=           # used when backend=azure\nKUBE_Q_AZURE_OPENAI_ENDPOINT=https://my-resource.openai.azure.com\nKUBE_Q_AZURE_OPENAI_DEPLOYMENT=        # deployment name, NOT model name\nKUBE_Q_AZURE_OPENAI_API_VERSION=2024-06-01\n\n# \u2500\u2500 Multi-cluster \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nKUBE_Q_CONTEXT=                        # initial kubectl context (also set live via /context)\nKUBE_Q_PROFILE=                        # load ~/.kube-q/profiles/&lt;name&gt;.env on top of defaults\nKUBE_Q_PLUGIN_DIR=                     # override plugin directory (default ~/.kube-q/plugins/)\n</code></pre>"},{"location":"configuration/#quick-setup","title":"Quick setup","text":""},{"location":"configuration/#minimal-user-level","title":"Minimal (user-level)","text":"<pre><code>mkdir -p ~/.kube-q\ncat &gt; ~/.kube-q/.env &lt;&lt;'EOF'\nKUBE_Q_URL=https://kube-q.example.com\nKUBE_Q_API_KEY=your-api-key\nEOF\n</code></pre>"},{"location":"configuration/#per-cluster-override","title":"Per-cluster override","text":"<p>Create <code>.env</code> in your cluster's working directory:</p> <pre><code># .env\nKUBE_Q_URL=https://kube-q.prod.example.com\nKUBE_Q_API_KEY=prod-key\nKUBE_Q_USER_NAME=alice\nKUBE_Q_MODEL=kubeintellect-v2\n</code></pre> <p>Run <code>kq</code> from that directory \u2014 it picks up the overrides automatically.</p>"},{"location":"configuration/#multiple-clusters","title":"Multiple clusters","text":"<pre><code>~/clusters/\n  prod/.env        \u2192 KUBE_Q_URL=https://kube-q.prod.example.com\n  staging/.env     \u2192 KUBE_Q_URL=https://kube-q.staging.example.com\n  dev/.env         \u2192 KUBE_Q_URL=http://localhost:8000\n</code></pre>"},{"location":"configuration/#custom-branding","title":"Custom branding","text":"<p>Operators can replace the startup banner and tagline:</p> <pre><code># .env\nKUBE_Q_LOGO=KubeIntellect\\nPowered by AI\nKUBE_Q_TAGLINE=\u00a9 2025 Acme Corp \u2014 Internal Use Only\n</code></pre> <p>Use <code>\\n</code> for line breaks in the logo. Set <code>KUBE_Q_LOGO=</code> (empty) to suppress the logo entirely.</p>"},{"location":"configuration/#files-written-by-kube-q","title":"Files written by kube-q","text":"Path Description <code>~/.kube-q/.env</code> User config (you create this) <code>~/.kube-q/profiles/&lt;name&gt;.env</code> Named profile fragments \u2014 see Profiles per cluster <code>~/.kube-q/plugins/*.py</code> User-authored slash commands \u2014 see Plugins <code>~/.kube-q/user-id</code> Auto-generated persistent user ID (<code>0600</code> permissions) <code>~/.kube-q/history.db</code> SQLite session history database <code>~/.kube-q/kube-q.log</code> Rotating log (5 MB \u00d7 3 files, written in <code>--debug</code> mode)"},{"location":"configuration/#backend-selection","title":"Backend selection","text":"<p>kube-q can talk to three backends with a single CLI \u2014 pick one per launch:</p> Backend What it hits Auth header <code>kube-q</code> (default) Your kube-q API server, <code>/v1/chat/completions</code> <code>Authorization: Bearer</code> (optional) <code>openai</code> <code>https://api.openai.com/v1/chat/completions</code> <code>Authorization: Bearer</code> <code>azure</code> <code>https://&lt;resource&gt;.openai.azure.com/openai/deployments/&lt;deployment&gt;/chat/completions?api-version=...</code> <code>api-key: &lt;key&gt;</code> <pre><code># Direct OpenAI\nkq --backend openai --openai-api-key sk-...\n\n# Azure OpenAI\nkq --backend azure \\\n   --azure-openai-api-key    ... \\\n   --azure-openai-endpoint   https://my-resource.openai.azure.com \\\n   --azure-openai-deployment gpt-4o\n</code></pre> <p>Validation catches missing keys up front:</p> <ul> <li><code>backend=openai</code> requires <code>KUBE_Q_OPENAI_API_KEY</code></li> <li><code>backend=azure</code> requires <code>KUBE_Q_AZURE_OPENAI_API_KEY</code>, <code>_ENDPOINT</code>, and <code>_DEPLOYMENT</code></li> </ul> <p>The backend is fixed for the lifetime of one REPL. <code>/state</code> shows the active backend label; the plain-HTTP warning is suppressed for <code>openai</code> / <code>azure</code>; and <code>/healthz</code> is skipped for non-<code>kube-q</code> backends (which don't expose one).</p>"},{"location":"configuration/#profiles-per-cluster","title":"Profiles per cluster","text":"<p>Profiles are <code>.env</code> fragments that bundle a full environment (backend + keys + kubectl context + custom branding) into one file. They live in <code>~/.kube-q/profiles/&lt;name&gt;.env</code> and are loaded between <code>~/.kube-q/.env</code> and <code>./.env</code> when selected.</p> <pre><code># Create a profile from a commented template\nkq config profile new prod\n\n# Edit ~/.kube-q/profiles/prod.env, e.g.:\n#   KUBE_Q_URL=https://kube-q.prod.example.com\n#   KUBE_Q_API_KEY=prod-key\n#   KUBE_Q_CONTEXT=prod-cluster\n#   KUBE_Q_USER_NAME=alice\n\n# Launch with the profile\nkq --profile prod\nKUBE_Q_PROFILE=prod kq           # equivalent\n\n# Manage profiles\nkq config profile list\nkq config profile show prod\nkq config profile delete staging\n</code></pre> <p>Precedence (high \u2192 low, highest wins):</p> <pre><code>CLI flag  &gt;  shell env  &gt;  ./.env  &gt;  ~/.kube-q/profiles/&lt;name&gt;.env  &gt;  ~/.kube-q/.env  &gt;  default\n</code></pre> <p><code>/profile</code> inside the REPL lists profiles and marks the active one; <code>/profile &lt;name&gt;</code> prints the restart command (switching profiles mid-session is not supported).</p>"},{"location":"configuration/#plugins","title":"Plugins","text":"<p>Drop a <code>.py</code> file into <code>~/.kube-q/plugins/</code> (or the directory named by <code>KUBE_Q_PLUGIN_DIR</code>) and call <code>register()</code> to add your own slash commands:</p> <pre><code># ~/.kube-q/plugins/hello.py\nfrom kube_q.plugins import register\n\n@register(\"/hello\", help=\"Say hello and show config\")\ndef hello(ctx):\n    ctx.print(f\"hi {ctx.cfg.user_name} \u2014 backend={ctx.cfg.backend_label}\")\n    ctx.print(f\"args passed: {ctx.args!r}\")\n</code></pre> <p>The <code>ctx</code> object exposes:</p> <ul> <li><code>args</code> \u2014 everything the user typed after the command (string)</li> <li><code>state</code> \u2014 the live <code>SessionState</code> (conversation id, messages, namespace, current context, HITL flag)</li> <li><code>cfg</code> \u2014 the current <code>ReplConfig</code> (display names, backend info, URL, \u2026)</li> <li><code>print(text)</code> \u2014 convenience for writing to the Rich console</li> <li><code>console</code> \u2014 the raw Rich console for advanced rendering</li> </ul> <p>Plugins run in-process with full Python access \u2014 only install plugins you trust. Import failures are logged and the REPL continues to start. <code>/plugins</code> lists what's loaded; plugin commands dispatch before the unknown-command typo-catcher, so they always win.</p>"},{"location":"deployment/","title":"Deployment &amp; Hosting","text":"<p>The kube-q web terminal is a Docker container. You need somewhere to run it so users can reach it from a browser.</p>"},{"location":"deployment/#hosting-options","title":"Hosting options","text":"Platform Free tier WebSockets Always-on Docker Verdict Oracle Cloud 4 CPU / 24 GB RAM \u2014 forever \u2713 \u2713 \u2713 Most generous, manual setup Fly.io 3 shared VMs \u2713 \u2713 \u2713 Best managed option Railway $5 credit/mo \u2713 \u2713 \u2713 Easiest setup Render Yes \u2713 \u2717 (sleeps after 15 min) \u2713 Bad for a terminal Koyeb Yes \u2713 \u2713 \u2713 Good alternative"},{"location":"deployment/#oracle-cloud-recommended-for-free-hosting","title":"Oracle Cloud (recommended for free hosting)","text":"<p>Oracle Cloud has the most generous free tier of any cloud provider \u2014 it never expires and never charges you. The only trade-off is manual setup.</p>"},{"location":"deployment/#what-you-get-free-forever","title":"What you get \u2014 free forever","text":"<ul> <li>4 ARM CPU cores + 24 GB RAM (Ampere A1) \u2014 use all 4 on one VM</li> <li>2 AMD VMs (1 CPU, 1 GB RAM each)</li> <li>200 GB storage</li> <li>No time limit, no credit card expiry</li> </ul> <p>A single ARM VM with 2 cores and 12 GB RAM is more than enough for the kube-q web terminal.</p>"},{"location":"deployment/#setup","title":"Setup","text":"<p>1. Create a free account</p> <p>Go to <code>cloud.oracle.com</code> \u2192 sign up \u2192 select your home region (pick one close to you \u2014 you cannot change it later).</p> <p>2. Create a free VM</p> <ul> <li>Compute \u2192 Instances \u2192 Create Instance</li> <li>Shape: Ampere A1 (ARM) \u2192 2 OCPUs, 12 GB RAM</li> <li>Image: Ubuntu 22.04</li> <li>Add your SSH public key</li> </ul> <p>3. Install Docker</p> <pre><code>ssh ubuntu@&lt;your-vm-ip&gt;\n\nsudo apt update &amp;&amp; sudo apt install -y docker.io\nsudo usermod -aG docker ubuntu\n# log out and back in for the group to take effect\n</code></pre> <p>4. Open port 3000</p> <p>In Oracle Console \u2192 Networking \u2192 VCN \u2192 Security List \u2192 add an ingress rule for TCP port 3000.</p> <p>Also allow it in the VM's local firewall:</p> <pre><code>sudo iptables -I INPUT -p tcp --dport 3000 -j ACCEPT\n</code></pre> <p>5. Run the container</p> <pre><code>docker run -d \\\n  --name kube-q \\\n  --restart always \\\n  -p 3000:3000 \\\n  -e KUBE_Q_URL=https://your-backend.com \\\n  -e KUBE_Q_API_KEY=your-key \\\n  ghcr.io/mskazemi/kube_q:latest\n</code></pre> <p>Open <code>http://&lt;your-vm-ip&gt;:3000</code> \u2014 done.</p>"},{"location":"deployment/#add-https-optional-but-recommended","title":"Add HTTPS (optional but recommended)","text":"<p>Install nginx and Certbot for a proper domain + TLS:</p> <pre><code>sudo apt install -y nginx certbot python3-certbot-nginx\n\n# Add your domain's A record pointing to the VM IP first, then:\nsudo certbot --nginx -d kube-q.example.com\n</code></pre> <p>Nginx config (<code>/etc/nginx/sites-available/kube-q</code>):</p> <pre><code>server {\n    listen 443 ssl;\n    server_name kube-q.example.com;\n\n    location / {\n        proxy_pass http://localhost:3000;\n        proxy_http_version 1.1;\n        proxy_set_header Upgrade $http_upgrade;\n        proxy_set_header Connection \"upgrade\";\n        proxy_set_header Host $host;\n    }\n}\n</code></pre> <p>Note</p> <p>The <code>Upgrade</code> and <code>Connection</code> headers are required for WebSocket (PTY) connections to work through nginx.</p>"},{"location":"deployment/#flyio-easiest-managed-option","title":"Fly.io (easiest managed option)","text":"<p>Fly.io deploys directly from your Dockerfile with one command and provides automatic HTTPS.</p> <pre><code># Install flyctl\ncurl -L https://fly.io/install.sh | sh\n\n# From the kube_q project root\nfly launch        # detects Dockerfile automatically\nfly secrets set KUBE_Q_URL=https://your-backend.com\nfly secrets set KUBE_Q_API_KEY=your-key\nfly deploy\n</code></pre> <p>Your app will be live at <code>https://kube-q.fly.dev</code> (or a name you choose). Free tier includes 3 shared VMs with automatic HTTPS and built-in WebSocket support.</p>"},{"location":"deployment/#railway","title":"Railway","text":"<p>Connect your GitHub repo and Railway auto-deploys on every push to <code>main</code>.</p> <ol> <li>Go to <code>railway.app</code> \u2192 New Project \u2192 Deploy from GitHub repo</li> <li>Select <code>MSKazemi/kube_q</code></li> <li>Add environment variables: <code>KUBE_Q_URL</code>, <code>KUBE_Q_API_KEY</code></li> <li>Railway detects the <code>Dockerfile</code> and builds automatically</li> </ol> <p>Free tier includes $5 credit/month \u2014 enough for light usage.</p>"},{"location":"deployment/#oracle-vs-flyio-comparison","title":"Oracle vs Fly.io comparison","text":"Oracle Cloud Fly.io Setup effort ~30 min manual 5 min, one command Resources 4 CPU / 24 GB RAM Shared, limited Cost Free forever Free tier, then pay-as-you-go HTTPS Manual (nginx + Certbot) Automatic Auto-deploy on push Manual (you set up CI) Built-in <p>Oracle is better long-term value (much more RAM/CPU, truly free forever). Fly.io is faster to get running with less ops work.</p>"},{"location":"faq/","title":"Frequently Asked Questions","text":"","tags":["reference"]},{"location":"faq/#general","title":"General","text":"","tags":["reference"]},{"location":"faq/#what-is-kube-q","title":"What is kube-q?","text":"<p>kube-q is an AI-powered terminal client for Kubernetes. It lets you query, debug, and manage any Kubernetes cluster by typing in plain English \u2014 no kubectl syntax required. It connects to a kube-q backend server that has access to your cluster.</p>","tags":["reference"]},{"location":"faq/#what-does-kube-q-do-exactly","title":"What does kube-q do, exactly?","text":"<p>You ask a question in the terminal. kube-q sends it to a kube-q backend, which uses an AI model to run <code>kubectl</code> commands, read pod logs, inspect events, and assemble an expert-level answer. The response streams back to your terminal in real time.</p>","tags":["reference"]},{"location":"faq/#is-kube-q-a-standalone-tool-or-does-it-need-a-server","title":"Is kube-q a standalone tool or does it need a server?","text":"<p>kube-q is a client. It requires a running kube-q backend server that has Kubernetes API access. The client (<code>kq</code>) connects to that server over HTTP/SSE. The server is a separate component \u2014 see Deployment &amp; Hosting for options.</p>","tags":["reference"]},{"location":"faq/#does-kube-q-ever-touch-my-cluster-directly","title":"Does kube-q ever touch my cluster directly?","text":"<p>No. The <code>kq</code> client only talks to the kube-q HTTP/SSE API. All cluster operations happen on the backend server. The client never has <code>kubeconfig</code> or direct cluster access.</p>","tags":["reference"]},{"location":"faq/#what-ai-models-does-kube-q-support","title":"What AI models does kube-q support?","text":"<p>That depends on your backend. The client sends a <code>model</code> parameter (default: <code>kubeintellect-v2</code>) \u2014 the backend decides which model to use. You can override the model name with <code>--model</code> or <code>KUBE_Q_MODEL</code>.</p>","tags":["reference"]},{"location":"faq/#installation","title":"Installation","text":"","tags":["reference"]},{"location":"faq/#what-python-version-is-required","title":"What Python version is required?","text":"<p>Python 3.12 or later. kube-q uses <code>match</code>/<code>case</code> structural pattern matching, <code>tomllib</code>, and other 3.12+ features.</p>","tags":["reference"]},{"location":"faq/#can-i-install-kube-q-without-polluting-my-system-python","title":"Can I install kube-q without polluting my system Python?","text":"<p>Yes \u2014 use pipx:</p> <pre><code>pipx install kube-q\n</code></pre> <p>This installs <code>kq</code> in an isolated virtualenv and puts it on your <code>PATH</code> automatically.</p>","tags":["reference"]},{"location":"faq/#how-do-i-upgrade-to-the-latest-version","title":"How do I upgrade to the latest version?","text":"pippipxHomebrew <pre><code>pip install --upgrade kube-q\n</code></pre> <pre><code>pipx upgrade kube-q\n</code></pre> <pre><code>brew upgrade kube-q\n</code></pre>","tags":["reference"]},{"location":"faq/#is-there-a-docker-image-i-can-use-instead","title":"Is there a Docker image I can use instead?","text":"<p>Yes \u2014 the Docker image bundles the full <code>kq</code> CLI plus a browser-based terminal:</p> <pre><code>docker run -p 3000:3000 \\\n  -e KUBE_Q_URL=https://kube-q.example.com \\\n  -e KUBE_Q_API_KEY=your-key \\\n  ghcr.io/mskazemi/kube_q:latest\n</code></pre> <p>Open <code>http://localhost:3000</code> for a browser REPL. See Web UI.</p>","tags":["reference"]},{"location":"faq/#configuration","title":"Configuration","text":"","tags":["reference"]},{"location":"faq/#where-does-kube-q-store-its-data","title":"Where does kube-q store its data?","text":"Path What's stored <code>~/.kube-q/.env</code> Your config (you create this) <code>~/.kube-q/user-id</code> Auto-generated persistent user ID <code>~/.kube-q/history.db</code> SQLite session history <code>~/.kube-q/kube-q.log</code> Debug log (only in <code>--debug</code> mode)","tags":["reference"]},{"location":"faq/#how-do-i-connect-to-multiple-clusters","title":"How do I connect to multiple clusters?","text":"<p>Create a <code>.env</code> file per cluster directory:</p> <pre><code>~/clusters/\n  prod/.env        \u2192 KUBE_Q_URL=https://kube-q.prod.example.com\n  staging/.env     \u2192 KUBE_Q_URL=https://kube-q.staging.example.com\n</code></pre> <p>Run <code>kq</code> from each directory and it picks up the local <code>.env</code> automatically.</p>","tags":["reference"]},{"location":"faq/#can-i-use-kube-q-without-an-api-key","title":"Can I use kube-q without an API key?","text":"<p>Yes, if your backend doesn't require authentication. Simply omit <code>KUBE_Q_API_KEY</code> and the <code>--api-key</code> flag.</p>","tags":["reference"]},{"location":"faq/#how-do-i-set-a-default-namespace","title":"How do I set a default namespace?","text":"<p>Either:</p> <ol> <li>Use <code>/ns &lt;name&gt;</code> inside the REPL to set it for that session.</li> <li>Or set it in your <code>.env</code> \u2014 there's no static <code>KUBE_Q_NAMESPACE</code> variable, but you can use a startup alias: <code>alias kq='kq --query \"/ns production\"'</code> is not ideal; instead just use <code>/ns</code> at the start of each session.</li> </ol>","tags":["reference"]},{"location":"faq/#usage","title":"Usage","text":"","tags":["reference"]},{"location":"faq/#does-kube-q-remember-past-conversations","title":"Does kube-q remember past conversations?","text":"<p>Yes. Every conversation is saved to a local SQLite database (<code>~/.kube-q/history.db</code>). Nothing is stored on the server.</p> <ul> <li>List sessions (table, exits): <code>kq --list</code></li> <li>Resume interactively: type <code>/sessions</code> (or <code>/resume</code>) in the REPL \u2014 arrow-key picker, Enter to resume in place, Esc to cancel; the stored transcript is replayed so you see the conversation before continuing</li> <li>Resume by ID from the shell: <code>kq --session-id &lt;id&gt;</code> \u2014 also replays the stored transcript on launch</li> <li>Search history: <code>kq --search \"pod crash\"</code> or <code>/search pod crash</code></li> </ul>","tags":["reference"]},{"location":"faq/#can-i-use-kube-q-in-scripts-and-ci","title":"Can I use kube-q in scripts and CI?","text":"<p>Yes \u2014 use <code>--query</code> mode:</p> <pre><code>kq --query \"list all failing pods\" --output plain\n</code></pre> <p><code>--output plain</code> disables Rich markdown rendering for clean stdout that pipes well. In <code>--query</code> mode, kube-q prints the response and exits immediately.</p>","tags":["reference"]},{"location":"faq/#what-file-types-can-i-attach-with","title":"What file types can I attach with <code>@</code>?","text":"<p><code>yaml</code>, <code>json</code>, <code>py</code>, <code>sh</code>, <code>go</code>, <code>tf</code>, <code>toml</code>, <code>js</code>, <code>ts</code>, <code>rs</code>, <code>java</code>, <code>xml</code>, <code>html</code>, <code>md</code>, <code>txt</code>, <code>log</code>, and others. The limit is 100 KB per file.</p>","tags":["reference"]},{"location":"faq/#is-the-conversation-context-sent-to-the-server-on-every-message","title":"Is the conversation context sent to the server on every message?","text":"<p>Yes. kube-q maintains a client-side message history and sends the full conversation on each request. This is how the AI understands follow-up questions. History is stored locally.</p>","tags":["reference"]},{"location":"faq/#can-i-fork-a-conversation-without-losing-the-original","title":"Can I fork a conversation without losing the original?","text":"<p>Yes \u2014 use <code>/branch</code>. It creates a new independent session at the current message count. The original is never modified. List forks with <code>/branches</code>.</p>","tags":["reference"]},{"location":"faq/#security-privacy","title":"Security &amp; Privacy","text":"","tags":["reference"]},{"location":"faq/#are-my-cluster-credentials-stored-anywhere-by-kube-q","title":"Are my cluster credentials stored anywhere by kube-q?","text":"<p>No. The <code>kq</code> client stores nothing about your cluster. All cluster access happens on the backend server. The client only stores session message history, token counts, and user preferences in <code>~/.kube-q/</code>.</p>","tags":["reference"]},{"location":"faq/#can-kube-q-execute-destructive-commands-without-my-approval","title":"Can kube-q execute destructive commands without my approval?","text":"<p>Only if the Human-in-the-Loop feature is disabled on the backend. When HITL is enabled, any destructive action (delete, scale, patch) is paused and shown to you \u2014 nothing runs until you type <code>/approve</code>.</p>","tags":["reference"]},{"location":"faq/#is-traffic-between-the-client-and-backend-encrypted","title":"Is traffic between the client and backend encrypted?","text":"<p>Use <code>https://</code> in <code>KUBE_Q_URL</code> to enable TLS. For custom CA certificates (corporate proxies, self-signed certs), use <code>--ca-cert /path/to/ca.pem</code>.</p>","tags":["reference"]},{"location":"faq/#troubleshooting","title":"Troubleshooting","text":"","tags":["reference"]},{"location":"faq/#kube-q-cant-connect-to-my-backend","title":"kube-q can't connect to my backend","text":"<p>Check your <code>KUBE_Q_URL</code> and that the backend is reachable:</p> <pre><code>curl -s $KUBE_Q_URL/health\n</code></pre> <p>See the full Troubleshooting Guide.</p>","tags":["reference"]},{"location":"faq/#the-response-cuts-off-mid-stream","title":"The response cuts off mid-stream","text":"<p>This is usually a server-side timeout. Try:</p> <pre><code>KUBE_Q_TIMEOUT=300 kq\n</code></pre>","tags":["reference"]},{"location":"faq/#im-getting-a-401-unauthorized-error","title":"I'm getting a 401 Unauthorized error","text":"<p>Your <code>KUBE_Q_API_KEY</code> is missing, wrong, or expired. See the Troubleshooting Guide.</p>","tags":["reference"]},{"location":"file-attachments/","title":"File Attachments","text":"<p>Embed a file's contents directly in your message using <code>@</code>:</p> <pre><code>what is wrong with this deployment? @deployment.yaml\ncompare these two configs: @old.yaml @new.yaml\nwhy is this pod failing? @pod.yaml @service.yaml\n</code></pre> <p>The file contents are embedded as a fenced code block and sent to the AI as part of your message. You can attach multiple files per message.</p>"},{"location":"file-attachments/#syntax","title":"Syntax","text":"<pre><code>@filename                  # relative to current directory\n@~/path/to/file.json       # home-relative\n@/absolute/path/to/file    # absolute path\n@\"path/with spaces.txt\"    # quote paths that contain spaces\n</code></pre> <p>The <code>@</code> reference can appear anywhere in the message:</p> <pre><code>what changed? @before.yaml @after.yaml\n@pod.yaml \u2014 is this resource request reasonable?\ncheck @deploy.yaml and tell me why the rollout is stuck\n</code></pre>"},{"location":"file-attachments/#supported-file-types","title":"Supported file types","text":"<p><code>yaml</code>, <code>json</code>, <code>py</code>, <code>sh</code>, <code>go</code>, <code>tf</code>, <code>toml</code>, <code>js</code>, <code>ts</code>, <code>rs</code>, <code>java</code>, <code>xml</code>, <code>html</code>, <code>md</code>, <code>txt</code>, <code>log</code>, and more.</p> <p>Limit: 100 KB per file. Files larger than 100 KB are rejected with an error message.</p>"},{"location":"file-attachments/#example-workflow","title":"Example workflow","text":"<pre><code>You&gt; what is wrong with this deployment? @deployment.yaml\n\nkube-q&gt; The deployment has resource limits set too low \u2014 the container\n        requests 128Mi memory but the JVM heap alone requires at least 512Mi...\n</code></pre> <pre><code>You&gt; @pod.yaml @configmap.yaml \u2014 why is the config not being picked up?\n\nkube-q&gt; The ConfigMap mount in the pod spec references 'app-config' but the\n        ConfigMap is named 'app-configuration' \u2014 there's a name mismatch...\n</code></pre>"},{"location":"hitl/","title":"Human-in-the-Loop (HITL)","text":"<p>When the AI backend requests approval before executing a potentially destructive or irreversible action, kube-q pauses and waits for your explicit confirmation. Nothing runs until you say so.</p>"},{"location":"hitl/#how-it-works","title":"How it works","text":"<p>When a HITL action is pending, kube-q displays an approval panel and changes the prompt to <code>HITL&gt;</code>:</p> <pre><code>\u256d\u2500 Action requires approval \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u256e\n\u2502 The following action is pending approval:       \u2502\n\u2502                                                 \u2502\n\u2502   kubectl delete deployment nginx --namespace   \u2502\n\u2502   production                                    \u2502\n\u2502                                                 \u2502\n\u2502 Type /approve to proceed or /deny to cancel.   \u2502\n\u2570\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u256f\nHITL&gt; \n</code></pre> <p>The AI backend decides which actions require approval \u2014 typically anything that modifies cluster state (deletes, restarts, scaling, patching).</p>"},{"location":"hitl/#commands","title":"Commands","text":"Command Effect <code>/approve</code> Execute the pending action \u2014 the AI continues <code>/deny</code> Cancel the pending action \u2014 nothing is applied <p>Once you approve or deny, the REPL returns to the normal prompt.</p>"},{"location":"hitl/#safety-guarantees","title":"Safety guarantees","text":"<ul> <li>kube-q never executes a HITL action without an explicit <code>/approve</code></li> <li>Closing the REPL (<code>Ctrl+D</code>, <code>/quit</code>) while a HITL action is pending is treated as a deny</li> <li>The action details (command, risk level, diff) are shown before you decide \u2014 what you see is exactly what will run</li> </ul>"},{"location":"hitl/#in-single-query-mode","title":"In single-query mode","text":"<p>HITL is not supported in <code>kq --query</code> (non-interactive) mode. If the backend raises a HITL request during a non-interactive query, the action will be denied automatically. Use the interactive REPL for workflows that may require approval.</p>"},{"location":"installation/","title":"Installation","text":"<p>Requires Python 3.12+.</p>"},{"location":"installation/#pip-recommended","title":"pip (recommended)","text":"<pre><code>pip install kube-q\n</code></pre> <p>Installs the <code>kq</code> command globally. Use a virtual environment or <code>pipx</code> to keep it isolated:</p> <pre><code>pipx install kube-q\n</code></pre>"},{"location":"installation/#homebrew","title":"Homebrew","text":"<pre><code>brew tap MSKazemi/kube-q\nbrew install kube-q\n</code></pre>"},{"location":"installation/#from-source","title":"From source","text":"<pre><code>git clone https://github.com/MSKazemi/kube_q\ncd kube_q\npip install -e .\n</code></pre> <p>Install with dev dependencies:</p> <pre><code>pip install -e \".[dev]\"\n</code></pre>"},{"location":"installation/#docker-web-terminal","title":"Docker (web terminal)","text":"<p>The Docker image bundles the full <code>kq</code> CLI and a browser-based terminal (Next.js + xterm.js). No local Python or Node.js install needed.</p> <pre><code>docker pull ghcr.io/mskazemi/kube_q:latest\n\ndocker run -p 3000:3000 \\\n  -e KUBE_Q_URL=https://kube-q.example.com \\\n  -e KUBE_Q_API_KEY=your-key \\\n  ghcr.io/mskazemi/kube_q:latest\n</code></pre> <p>Open <code>http://localhost:3000</code> to get a full <code>kq</code> terminal in the browser.</p> <p>See Web UI for the complete Docker setup guide.</p>"},{"location":"installation/#verify","title":"Verify","text":"<pre><code>kq --version\n</code></pre>"},{"location":"installation/#dependencies","title":"Dependencies","text":"<p>kube-q installs only lightweight, pure-Python dependencies:</p> Package Purpose <code>httpx</code> HTTP client (streaming SSE, TLS, proxy support) <code>rich</code> Terminal rendering (markdown, syntax highlighting, tables) <code>prompt_toolkit</code> REPL input (Tab completion, history, multi-line) <code>pygments</code> Syntax highlighting for code blocks <code>pydantic</code> Event model validation"},{"location":"quickstart/","title":"Quick Start","text":"<p>Get kube-q running and talk to your first cluster in under 5 minutes.</p>","tags":["getting-started"]},{"location":"quickstart/#prerequisites","title":"Prerequisites","text":"<p>Before you begin, make sure you have:</p> <ul> <li> Python 3.12 or later</li> <li> A running kube-q backend (the server your cluster is connected to)</li> <li> The backend URL and API key (if auth is enabled)</li> </ul> <p>Don't have a backend yet?</p> <p>kube-q is a client. It connects to a kube-q server that has access to your Kubernetes cluster. If you're setting up your own server, see Deployment &amp; Hosting.</p>","tags":["getting-started"]},{"location":"quickstart/#step-1-install","title":"Step 1 \u2014 Install","text":"pipx (recommended) pip Homebrew From source <p>pipx installs <code>kq</code> in an isolated virtualenv and puts it on your <code>PATH</code> \u2014 no pollution of your system Python. Works on Linux, macOS, and Windows.</p> <pre><code>pip install pipx       # skip if you already have pipx\npipx ensurepath        # adds pipx and installed tools to PATH (all platforms)\n</code></pre> <p>Then restart your shell (or open a new terminal), and:</p> <pre><code>pipx install kube-q\nkq --version\n</code></pre> <p>macOS alternative</p> <p>On macOS, prefer <code>brew install pipx &amp;&amp; pipx ensurepath</code> \u2014 Homebrew keeps pipx itself updated.</p> <pre><code>pip install kube-q\nkq --version\n</code></pre> <pre><code>brew tap MSKazemi/kube-q\nbrew install kube-q\nkq --version\n</code></pre> <pre><code>git clone https://github.com/MSKazemi/kube_q\ncd kube_q\npip install -e .\nkq --version\n</code></pre> <p>You should see something like:</p> <pre><code>kube-q 1.4.0\n</code></pre>","tags":["getting-started"]},{"location":"quickstart/#step-2-configure-your-backend-url","title":"Step 2 \u2014 Configure your backend URL","text":"<p>Save your connection settings once so you never need to type them on every run.</p> <p>Option A \u2014 from inside the REPL (easiest, settings take effect immediately):</p> <pre><code>/config set url=https://kube-q.example.com\n/config set api_key=your-api-key-here\n</code></pre> <p>Option B \u2014 before launching (edit the file directly):</p> <pre><code>mkdir -p ~/.kube-q\ncat &gt; ~/.kube-q/.env &lt;&lt; 'EOF'\nKUBE_Q_URL=https://kube-q.example.com\nKUBE_Q_API_KEY=your-api-key-here\nEOF\n</code></pre> <p>Multiple clusters?</p> <p>Use per-directory <code>.env</code> files \u2014 see Configuration \u2192 Multiple clusters.</p>","tags":["getting-started"]},{"location":"quickstart/#step-3-start-the-repl","title":"Step 3 \u2014 Start the REPL","text":"<pre><code>kq\n</code></pre> <p>On first run, kube-q checks your backend is reachable, then drops you into the interactive REPL:</p> <pre><code> _          _                                 \n| | ___   _| |__   ___       __ _\n| |/ / | | | '_ \\ / _ \\     / _` |\n|   &lt;| |_| | |_) |  __/    | (_| |\n|_|\\_\\\\__,_|_.__/ \\___|     \\__, |\n                              |___/\nkube-q v1.4.0  |  session d4e91c  |  Connected to kube-q.example.com\n\nYou&gt;\n</code></pre>","tags":["getting-started"]},{"location":"quickstart/#step-4-ask-your-first-question","title":"Step 4 \u2014 Ask your first question","text":"<p>Just type naturally. No special syntax.</p> <pre><code>You&gt; show me all pods that are not running\n</code></pre> <pre><code>You&gt; what namespaces exist in this cluster?\n</code></pre> <pre><code>You&gt; why is the nginx deployment in staging failing?\n</code></pre> <p>kube-q streams the response in real time as tokens arrive from the backend.</p>","tags":["getting-started"]},{"location":"quickstart/#step-5-try-the-key-commands","title":"Step 5 \u2014 Try the key commands","text":"","tags":["getting-started"]},{"location":"quickstart/#set-a-namespace-context","title":"Set a namespace context","text":"<p>Instead of mentioning the namespace in every message, set it once:</p> <pre><code>You&gt; /ns production\n</code></pre> <p>Now all your questions are scoped to <code>production</code> automatically.</p>","tags":["getting-started"]},{"location":"quickstart/#attach-a-file","title":"Attach a file","text":"<p>Reference a YAML manifest or log file by prefixing with <code>@</code>:</p> <pre><code>You&gt; what is wrong with this deployment? @deployment.yaml\n</code></pre>","tags":["getting-started"]},{"location":"quickstart/#search-past-sessions","title":"Search past sessions","text":"<pre><code>You&gt; /search pod crash loop\n</code></pre> <p>Or from the terminal:</p> <pre><code>kq --search \"oom killed\"\n</code></pre>","tags":["getting-started"]},{"location":"quickstart/#see-token-usage","title":"See token usage","text":"<pre><code>You&gt; /tokens\n</code></pre>","tags":["getting-started"]},{"location":"quickstart/#save-the-conversation","title":"Save the conversation","text":"<pre><code>You&gt; /save\n</code></pre> <p>Saves a Markdown file of the full session to your current directory.</p>","tags":["getting-started"]},{"location":"quickstart/#step-6-exit","title":"Step 6 \u2014 Exit","text":"<pre><code>You&gt; /quit\n</code></pre> <p>Or press Ctrl+D.</p>","tags":["getting-started"]},{"location":"quickstart/#whats-next","title":"What's next?","text":"<ul> <li> <p> Configuration</p> <p><code>.env</code> files, environment variables, multi-cluster setups.</p> <p> Configuration</p> </li> <li> <p> All slash commands</p> <p>Complete reference for every <code>/command</code> available in the REPL.</p> <p> In-REPL Commands</p> </li> <li> <p> Human-in-the-Loop</p> <p>How kube-q handles destructive actions and asks for approval.</p> <p> HITL Guide</p> </li> <li> <p> Browser Terminal</p> <p>Run kube-q in any browser \u2014 no local install needed.</p> <p> Web UI</p> </li> </ul>","tags":["getting-started"]},{"location":"sdk/","title":"Python SDK","text":"<p><code>kube_q.core</code> exposes a typed SDK you can use directly in scripts, notebooks, and other tools \u2014 no CLI required.</p>"},{"location":"sdk/#installation","title":"Installation","text":"<pre><code>pip install kube-q\n</code></pre>"},{"location":"sdk/#kubeqclient","title":"KubeQClient","text":"<pre><code>from kube_q.core.client import KubeQClient\n\nclient = KubeQClient(\n    url=\"http://localhost:8000\",\n    api_key=\"your-key\",           # optional\n    ca_cert=\"/path/to/ca.pem\",    # optional, for custom TLS\n    timeout=120.0,\n    model=\"kubeintellect-v2\",\n)\n</code></pre>"},{"location":"sdk/#non-streaming-query","title":"Non-streaming query","text":"<pre><code>result = client.query(\n    \"why are my pods failing?\",\n    conversation_id=\"abc123\",     # optional, for multi-turn\n    user_id=\"alice\",              # optional\n)\nprint(result[\"text\"])\n</code></pre>"},{"location":"sdk/#streaming","title":"Streaming","text":"<pre><code>from kube_q.core.events import TokenEvent, StatusEvent, FinalEvent\n\nfor event in client.stream(\"list all deployments in default namespace\"):\n    match event:\n        case TokenEvent(data=d):\n            print(d.content, end=\"\", flush=True)\n        case StatusEvent(data=d):\n            print(f\"\\n[{d.phase}] {d.message}\")\n        case FinalEvent():\n            break\n</code></pre>"},{"location":"sdk/#health-check","title":"Health check","text":"<pre><code>healthy = client.health()    # returns True / False\n</code></pre>"},{"location":"sdk/#asynckubeqclient","title":"AsyncKubeQClient","text":"<p>An async variant using <code>httpx.AsyncClient</code> for non-blocking use in web servers and async frameworks:</p> <pre><code>import asyncio\nfrom kube_q.core.client import AsyncKubeQClient\nfrom kube_q.core.events import TokenEvent, FinalEvent\n\nasync def ask(question: str) -&gt; str:\n    async with AsyncKubeQClient(url=\"http://localhost:8000\") as client:\n        chunks = []\n        async for event in client.stream(question):\n            match event:\n                case TokenEvent(data=d):\n                    chunks.append(d.content)\n                case FinalEvent():\n                    break\n        return \"\".join(chunks)\n\nprint(asyncio.run(ask(\"show all namespaces\")))\n</code></pre>"},{"location":"sdk/#event-types","title":"Event types","text":"<p>All backend events are modelled as a typed Pydantic discriminated union in <code>kube_q.core.events</code>:</p> Event type Key fields <code>TokenEvent</code> <code>data.content</code>, <code>data.role</code> <code>StatusEvent</code> <code>data.phase</code>, <code>data.message</code> <code>ToolCallEvent</code> <code>data.tool_name</code>, <code>data.args</code>, <code>data.call_id</code>, <code>data.dry_run</code> <code>ToolResultEvent</code> <code>data.call_id</code>, <code>data.ok</code>, <code>data.summary</code>, <code>data.truncated</code> <code>HitlRequestEvent</code> <code>data.action</code>, <code>data.risk</code>, <code>data.diff</code>, <code>data.approval_id</code> <code>UsageEvent</code> <code>data.prompt_tokens</code>, <code>data.completion_tokens</code>, <code>data.total_tokens</code>, <code>data.model</code> <code>FinalEvent</code> <code>data.content</code>, <code>data.usage</code>, <code>data.elapsed_ms</code> <code>ErrorEvent</code> <code>data.code</code>, <code>data.message</code>, <code>data.retryable</code>"},{"location":"sdk/#retry-behaviour","title":"Retry behaviour","text":"<p>Both <code>KubeQClient</code> and <code>AsyncKubeQClient</code> apply automatic retries with exponential back-off for transient network errors (<code>httpx.TransportError</code>). The retry schedule is <code>[1s, 3s, 5s]</code> before giving up and raising.</p> <p>Non-retryable errors (4xx, 5xx) are raised immediately.</p>"},{"location":"session-history/","title":"Session History","text":"<p>kube-q saves every conversation to a local SQLite database at <code>~/.kube-q/history.db</code>. Nothing is sent to or stored on the server \u2014 this is a local-only mirror. The database never affects your cluster.</p>"},{"location":"session-history/#listing-sessions","title":"Listing sessions","text":"<pre><code>kq --list\n</code></pre> <p>Shows the 20 most recent sessions with ID, title, message count, token total, and last-used time (non-interactive table, exits after printing).</p>"},{"location":"session-history/#resuming-a-session","title":"Resuming a session","text":""},{"location":"session-history/#from-the-repl-interactive-picker","title":"From the REPL \u2014 interactive picker","text":"<pre><code>/sessions       (or /resume)\n</code></pre> <p>Opens a full-screen picker of the 20 most recent sessions. Use \u2191/\u2193 to navigate, Enter to resume the highlighted session in place, Esc to cancel. Each row shows <code>updated \u00b7 title \u00b7 msg count \u00b7 tokens \u00b7 namespace \u00b7 short id</code>.</p> <p>On selection, kube-q:</p> <ol> <li>Swaps the active conversation ID.</li> <li>Hydrates the full message history from <code>~/.kube-q/history.db</code>.</li> <li>Re-renders the stored transcript (user turns in green, assistant turns as markdown, separated by a rule showing the message count) so you see the whole conversation before continuing.</li> <li>Restores the kube context that was active when the session was last used \u2014 so you're automatically pointed back at the same cluster. A dim <code>Context restored to \u2026</code> line confirms it. Override any time with <code>/context &lt;name&gt;</code>.</li> <li>Clears any pending HITL flag \u2014 you're ready to send the next message.</li> </ol> <p>Picker shows the context</p> <p>Each row in the picker and <code>kq --list</code> table now includes the session's kube context (<code>ctx=prod-cluster</code>) so you can tell at a glance which cluster a session was working against.</p> <p>No restart is required. The picker renders identically in the web UI because the browser is a pure PTY relay to the <code>kq</code> process.</p>"},{"location":"session-history/#from-the-shell-by-session-id","title":"From the shell \u2014 by session ID","text":"<pre><code>kq --session-id &lt;id&gt;\n</code></pre> <p>Launches kube-q directly into the chosen session. The stored transcript is replayed at startup the same way it is in the REPL picker, and the stored kube context is restored onto <code>state.current_context</code> so the next message is routed to the same cluster. The session ID is shown in <code>kq --list</code> output. If you pass <code>--context &lt;name&gt;</code> explicitly on the command line, your flag wins over the stored value.</p>"},{"location":"session-history/#replaying-the-current-session-on-demand","title":"Replaying the current session on demand","text":"<pre><code>/history              # all messages\n/history &lt;N&gt;          # last N messages\n/history &lt;X-Y&gt;        # messages X through Y (1-indexed, inclusive)\n/history #&lt;N&gt;         # just message #N\n</code></pre> <p>Useful when the transcript has scrolled off the top of the terminal, or when you want to quote a specific earlier exchange. Every line is prefixed with a <code>[#N]</code> marker so you can jump back by number:</p> <pre><code>/history 5            # review the last 5 turns\n/history 1-4          # re-read the opening exchange\n/history #7           # pull back just assistant message #7\n</code></pre> <p>Malformed or out-of-range specs print a usage hint \u2014 no state is changed. <code>/history</code> operates purely on the session already in memory; it does not hit the database.</p>"},{"location":"session-history/#full-text-search","title":"Full-text search","text":"<pre><code>kq --search \"deployment rollback\"\nkq --search \"pods AND crash\"\nkq --search \"oom killed OR crash loop\"\n</code></pre> <p>Powered by SQLite FTS5. Results show session title, matched message excerpt with highlighted terms, and the session ID. Supports FTS5 boolean syntax:</p> Syntax Meaning <code>pods crash</code> both words anywhere <code>pods AND crash</code> both words (explicit) <code>pods OR crash</code> either word <code>pods NOT staging</code> pods but not staging <code>\"crash loop\"</code> exact phrase <p>Inside the REPL: <code>/search &lt;query&gt;</code> works the same way.</p>"},{"location":"session-history/#conversation-branching","title":"Conversation branching","text":"<pre><code>/branch\n</code></pre> <p>Forks the current conversation at the current message count into a new independent session. The original session is never modified \u2014 you get a clean fork you can take in a different direction.</p> <pre><code>/branches        \u2014 list all forks of (and siblings of) this session\n/title &lt;text&gt;    \u2014 rename the current session\n</code></pre> <p>Branches show up in <code>kq --list</code> as regular sessions. Search finds them automatically.</p>"},{"location":"session-history/#deleting-a-session","title":"Deleting a session","text":"<pre><code>/forget\n</code></pre> <p>Deletes the current session from local history. Server-side state (if any) is not affected.</p>"},{"location":"session-history/#database-details","title":"Database details","text":"Property Value Path <code>~/.kube-q/history.db</code> Format SQLite 3, WAL mode Schema version v3 (auto-migrates from v1 and v2) What's stored Session metadata, messages, token counts, FTS index What's NOT stored Cluster credentials, API keys, server state <p>Schema migrations run automatically at startup \u2014 old databases from v1.0.0 are upgraded transparently.</p>"},{"location":"token-tracking/","title":"Token Tracking","text":"<p>kube-q tracks token usage per response and per session, so you always know what a conversation costs.</p>"},{"location":"token-tracking/#response-footer","title":"Response footer","text":"<p>After every response that includes usage data, kube-q shows a footer:</p> <pre><code>kube-q  (1.2s \u00b7 460 tokens)\n</code></pre> <p>This is the elapsed response time and the total tokens for that exchange (prompt + completion). If the server doesn't emit a <code>usage</code> block, the footer is omitted silently \u2014 no errors.</p>"},{"location":"token-tracking/#session-summary","title":"Session summary","text":"<p>Use <code>/tokens</code> or <code>/cost</code> inside the REPL for a full breakdown:</p> <pre><code>\u250c\u2500 Token Usage \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n\u2502 This session:                         \u2502\n\u2502   Prompt:     1,240 tokens            \u2502\n\u2502   Completion: 3,890 tokens            \u2502\n\u2502   Total:      5,130 tokens            \u2502\n\u2502   Requests:   8                       \u2502\n\u2502   Est. cost:  $0.0312                 \u2502\n\u2502                                       \u2502\n\u2502 Last response:                        \u2502\n\u2502   120 in \u2192 340 out ($0.0024)          \u2502\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n</code></pre> <p>Cost estimates are labeled \"Est.\" \u2014 they are approximations based on per-model rate tables, not exact billing figures.</p>"},{"location":"token-tracking/#session-list","title":"Session list","text":"<p><code>kq --list</code> shows a Tokens column for every session:</p> <pre><code>ID        Title                     Messages  Tokens   Last used\n\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500  \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500  \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500  \u2500\u2500\u2500\u2500\u2500\u2500   \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nabc123    Debug failing pods        12        8,430    2 hours ago\ndef456    Scale deployment prod     5         2,100    yesterday\n</code></pre>"},{"location":"token-tracking/#built-in-rate-table","title":"Built-in rate table","text":"Model Prompt Completion <code>kubeintellect-v2</code> $0.003 / 1K $0.006 / 1K <code>gpt-4o</code> $0.005 / 1K $0.015 / 1K <code>gpt-4o-mini</code> $0.00015 / 1K $0.0006 / 1K <code>claude-sonnet-4-6</code> $0.003 / 1K $0.015 / 1K"},{"location":"token-tracking/#custom-rates","title":"Custom rates","text":"<p>Override rates for any model via environment variables or <code>.env</code>:</p> <pre><code>KUBE_Q_COST_PER_1K_PROMPT=0.002\nKUBE_Q_COST_PER_1K_COMPLETION=0.008\n</code></pre> <p>This is useful when running a self-hosted model or when the backend uses non-standard pricing.</p>"},{"location":"troubleshooting/","title":"Troubleshooting","text":"<p>Solutions to the most common problems. If your issue isn't here, open an issue on GitHub.</p>","tags":["reference"]},{"location":"troubleshooting/#connection-problems","title":"Connection Problems","text":"","tags":["reference"]},{"location":"troubleshooting/#connection-refused-or-cannot-connect-to-backend","title":"<code>Connection refused</code> or <code>Cannot connect to backend</code>","text":"<p>Symptom: kube-q exits at startup with a message like:</p> <pre><code>\u2717  Cannot reach backend at https://api.kubeintellect.com (Connection refused)\n    Entering offline mode \u2014 use /url &lt;URL&gt; or /config set url=&lt;URL&gt; to point at a running backend.\n</code></pre> <p>Causes and fixes:</p> Wrong URLBackend not runningFirewall / networkSkip the retry loop <p>Check your <code>KUBE_Q_URL</code> is correct and the port matches what the backend is listening on:</p> <pre><code>echo $KUBE_Q_URL\ncurl -s http://localhost:8000/health\n</code></pre> <p>Start your kube-q backend server. If you're using Docker:</p> <pre><code>docker ps | grep kube-q\n</code></pre> <p>Ensure the host and port are reachable from your machine. If the backend is on a remote host, check security group / firewall rules.</p> <p>If you want to fail fast instead of waiting:</p> <pre><code>KUBE_Q_SKIP_HEALTH_CHECK=true kq\n</code></pre>","tags":["reference"]},{"location":"troubleshooting/#ssl-certificate_verify_failed","title":"<code>SSL: CERTIFICATE_VERIFY_FAILED</code>","text":"<p>Symptom:</p> <pre><code>httpx.ConnectError: [SSL: CERTIFICATE_VERIFY_FAILED] certificate verify failed\n</code></pre> <p>Fix: Pass your custom CA certificate:</p> <pre><code>kq --ca-cert /path/to/ca.pem\n</code></pre> <p>Or set it in <code>.env</code>:</p> <pre><code># ~/.kube-q/.env\nKUBE_Q_CA_CERT=/path/to/ca.pem\n</code></pre> <p>This is common with corporate proxies, self-signed certificates, or internal CAs.</p>","tags":["reference"]},{"location":"troubleshooting/#startup-keeps-retrying-for-minutes","title":"Startup keeps retrying for minutes","text":"<p>By default, kube-q retries the health check for up to 5 minutes, waiting for the backend to come online. To skip this:</p> <pre><code>KUBE_Q_SKIP_HEALTH_CHECK=true kq\n# or\nkq --no-health-check\n</code></pre>","tags":["reference"]},{"location":"troubleshooting/#authentication-errors","title":"Authentication Errors","text":"","tags":["reference"]},{"location":"troubleshooting/#401-unauthorized","title":"<code>401 Unauthorized</code>","text":"<p>Symptom:</p> <pre><code>\u2717  Authentication required. Set KUBE_Q_API_KEY or use --api-key.\n</code></pre> <p>Fix: Provide your API key:</p> CLI flagEnvironment variable.env file <pre><code>kq --api-key your-key-here\n</code></pre> <pre><code>export KUBE_Q_API_KEY=your-key-here\nkq\n</code></pre> <pre><code># ~/.kube-q/.env\nKUBE_Q_API_KEY=your-key-here\n</code></pre>","tags":["reference"]},{"location":"troubleshooting/#403-forbidden","title":"<code>403 Forbidden</code>","text":"<p>Your API key is recognized but doesn't have permission to perform the requested action. Contact your kube-q server administrator to check key permissions.</p>","tags":["reference"]},{"location":"troubleshooting/#response-streaming-issues","title":"Response &amp; Streaming Issues","text":"","tags":["reference"]},{"location":"troubleshooting/#response-cuts-off-mid-stream","title":"Response cuts off mid-stream","text":"<p>The HTTP connection timed out before the backend finished responding. Increase the timeout:</p> <pre><code>KUBE_Q_TIMEOUT=300 kq\n</code></pre> <p>Or set it permanently:</p> <pre><code># ~/.kube-q/.env\nKUBE_Q_TIMEOUT=300\n</code></pre>","tags":["reference"]},{"location":"troubleshooting/#no-streaming-response-appears-all-at-once-after-a-long-wait","title":"No streaming \u2014 response appears all at once after a long wait","text":"<p>The server may have disabled SSE, or a proxy is buffering the response. Check:</p> <ol> <li>Is there a proxy (nginx, Traefik, AWS ALB) between you and the backend? Ensure it is configured for SSE:</li> <li>nginx: <code>proxy_buffering off; proxy_cache off;</code></li> <li>AWS ALB: use HTTP/1.1 target group</li> <li>Try disabling streaming to confirm the backend is responsive:    <pre><code>kq --no-stream\n</code></pre></li> </ol>","tags":["reference"]},{"location":"troubleshooting/#event-parse-error-in-the-response","title":"<code>Event parse error</code> in the response","text":"<p>kube-q received a malformed SSE event from the backend. Enable debug logging to see the raw bytes:</p> <pre><code>kq --debug\n</code></pre> <p>Check <code>~/.kube-q/kube-q.log</code> for the raw HTTP traffic.</p>","tags":["reference"]},{"location":"troubleshooting/#repl-display-issues","title":"REPL &amp; Display Issues","text":"","tags":["reference"]},{"location":"troubleshooting/#the-terminal-looks-broken-garbled","title":"The terminal looks broken / garbled","text":"<p>kube-q uses Rich for rendering. Make sure your terminal supports:</p> <ul> <li>UTF-8 encoding \u2014 check with <code>echo $LANG</code> (should end in <code>.UTF-8</code>)</li> <li>A modern terminal \u2014 iTerm2, Warp, Windows Terminal, GNOME Terminal, or any xterm-compatible emulator</li> </ul> <p>If you need plain text output (e.g. inside a script or on a minimal tty):</p> <pre><code>kq --output plain\n</code></pre>","tags":["reference"]},{"location":"troubleshooting/#tab-completion-doesnt-work","title":"Tab completion doesn't work","text":"<p>Tab completion for slash commands is provided by <code>prompt_toolkit</code>. It only works in interactive REPL mode, not in <code>--query</code> mode.</p> <p>Ensure you are running a real interactive terminal, not inside a pipe or subshell. If you launched <code>kq</code> via <code>$(...)</code> or <code>echo | kq</code>, completion will be disabled.</p>","tags":["reference"]},{"location":"troubleshooting/#search-returns-no-results-even-though-i-had-sessions","title":"<code>/search</code> returns no results even though I had sessions","text":"<p>The FTS5 search index was introduced in schema v3. If your <code>history.db</code> is from an older version, run kube-q once to trigger the automatic schema migration:</p> <pre><code>kq --no-health-check\n</code></pre> <p>Then try <code>/search</code> again. The migration backfills the FTS index from existing messages.</p>","tags":["reference"]},{"location":"troubleshooting/#session-history-is-gone","title":"Session history is gone","text":"<p>kube-q stores history in <code>~/.kube-q/history.db</code>. Check:</p> <pre><code>ls -lh ~/.kube-q/history.db\n</code></pre> <p>If the file was deleted or the path changed (e.g., you changed <code>HOME</code>), history won't be available.</p> <p>History is local only</p> <p>History is never uploaded to the server. There is no cloud backup. If <code>history.db</code> is deleted, those sessions are gone.</p>","tags":["reference"]},{"location":"troubleshooting/#file-attachments","title":"File Attachments","text":"","tags":["reference"]},{"location":"troubleshooting/#file-not-found-myfileyaml","title":"<code>File not found: @myfile.yaml</code>","text":"<ul> <li>The path is resolved relative to your current working directory at the time you run <code>kq</code>. If the file is elsewhere, use an absolute path: <code>@/home/user/configs/myfile.yaml</code>.</li> <li>Paths with spaces must be quoted: <code>@\"path/with spaces.yaml\"</code>.</li> </ul>","tags":["reference"]},{"location":"troubleshooting/#file-too-large-error","title":"<code>File too large</code> error","text":"<p>Files over 100 KB are rejected. Split large files or trim them to the relevant section before attaching.</p>","tags":["reference"]},{"location":"troubleshooting/#debug-logging","title":"Debug Logging","text":"<p>For any hard-to-diagnose problem, run kube-q in debug mode:</p> <pre><code>kq --debug\n</code></pre> <p>This logs all raw HTTP requests and responses to:</p> <ul> <li>stderr (visible in your terminal)</li> <li><code>~/.kube-q/kube-q.log</code> (rotating, 5 MB \u00d7 3 files)</li> </ul> <p>The log shows the full SSE event stream, headers, and retry attempts \u2014 useful for diagnosing proxy issues, TLS problems, or unexpected server responses.</p>","tags":["reference"]},{"location":"troubleshooting/#still-stuck","title":"Still stuck?","text":"<ol> <li>Check the FAQ for common questions</li> <li>Search GitHub Issues \u2014 your problem may already be reported</li> <li>Open a new issue with:</li> <li>kube-q version (<code>kq --version</code>)</li> <li>The exact error message</li> <li>The output of <code>kq --debug</code> with sensitive values redacted</li> </ol>","tags":["reference"]},{"location":"web-ui/","title":"Web UI","text":"<p>kube-q includes a browser-based terminal that runs the full <code>kq</code> REPL in any browser. No client-side logic is duplicated \u2014 xterm.js renders bytes relayed from a real <code>kq</code> process running in a pseudo-terminal (PTY) on the server. All slash commands, streaming, HITL, session history, and file attachments work exactly as in the terminal.</p> <pre><code>Browser (xterm.js) \u2190\u2192 WebSocket (/pty-ws) \u2190\u2192 node-pty \u2190\u2192 kq process\n</code></pre>"},{"location":"web-ui/#docker-recommended","title":"Docker (recommended)","text":"<p>The Docker image bundles everything: the Python <code>kq</code> CLI, the Next.js frontend, and the PTY WebSocket server.</p>"},{"location":"web-ui/#run","title":"Run","text":"<pre><code>docker run -p 3000:3000 \\\n  -e KUBE_Q_URL=https://kube-q.example.com \\\n  -e KUBE_Q_API_KEY=your-key \\\n  ghcr.io/mskazemi/kube_q:latest\n</code></pre> <p>Open <code>http://localhost:3000</code>. Each browser tab gets its own independent <code>kq</code> process.</p>"},{"location":"web-ui/#environment-variables","title":"Environment variables","text":"Variable Required Description <code>KUBE_Q_URL</code> Yes kube-q API base URL <code>KUBE_Q_API_KEY</code> When auth is enabled API key for the backend <code>KUBE_Q_MODEL</code> No Model name (default: <code>kubeintellect-v2</code>) <code>KUBE_Q_USER_NAME</code> No Display name shown in the prompt <code>PORT</code> No HTTP port (default: <code>3000</code>) <code>KUBE_Q_SKIP_HEALTH_CHECK</code> No Set <code>true</code> to skip startup health check <code>PTY_AUTH_TOKEN</code> No Require clients to supply this token on the WebSocket; when unset, auth is disabled"},{"location":"web-ui/#build-your-own-image","title":"Build your own image","text":"<pre><code>git clone https://github.com/MSKazemi/kube_q\ncd kube_q\ndocker build -t kube-q-web .\ndocker run -p 3000:3000 -e KUBE_Q_URL=... kube-q-web\n</code></pre> <p>The Dockerfile is a multi-stage build: Node builder compiles Next.js, runtime stage installs <code>kube-q</code> from PyPI and copies the built app. No <code>.env</code> is baked into the image \u2014 inject everything at runtime.</p>"},{"location":"web-ui/#iframe-embedding","title":"iframe embedding","text":"<p>The web terminal can be embedded in any parent page. The <code>NEXT_PUBLIC_BASE_PATH</code> variable relocates the app to a sub-path:</p> <pre><code>docker run -p 3000:3000 \\\n  -e KUBE_Q_URL=https://kube-q.example.com \\\n  -e KUBE_Q_API_KEY=your-key \\\n  -e NEXT_PUBLIC_BASE_PATH=/kq \\\n  ghcr.io/mskazemi/kube_q:latest\n</code></pre> <p>The app is then available at <code>http://host:3000/kq</code> and can be embedded:</p> <pre><code>&lt;iframe src=\"http://host:3000/kq\" width=\"100%\" height=\"600px\"&gt;&lt;/iframe&gt;\n</code></pre> <p><code>Content-Security-Policy: frame-ancestors *</code> and <code>X-Frame-Options: ALLOWALL</code> are set on all responses, so embedding from any origin works by default.</p>"},{"location":"web-ui/#development-setup","title":"Development setup","text":"<p>Run Next.js and the PTY server locally (Node.js 20+ required, and <code>kq</code> must be installed):</p> <pre><code>cd web\nnpm install\nnpm run dev        # starts Next.js on :3000 and PTY server on :3001\n</code></pre> <p>Open <code>http://localhost:3000</code>.</p> <p>To point the PTY at a different <code>kube-q</code> binary:</p> <pre><code>PTY_CMD=/path/to/kq npm run dev\n</code></pre>"},{"location":"web-ui/#production-build-no-docker","title":"Production build (no Docker)","text":"<pre><code>cd web\nnpm run build\nnpm run start      # Next.js + PTY WebSocket on the same port\n</code></pre> <p><code>server.mjs</code> upgrades <code>/pty-ws</code> connections and handles them with node-pty. No second port needed.</p>"},{"location":"web-ui/#download-conversation","title":"Download conversation","text":"<p>The toolbar has a \u2b07 Download button that exports the xterm.js scrollback buffer as a Markdown file directly to your browser \u2014 no server round-trip needed.</p>"},{"location":"web-ui/#connection-status-auto-reconnect","title":"Connection status &amp; auto-reconnect","text":"<p>A coloured status dot in the toolbar reflects the live WebSocket state:</p> Status Meaning <code>connecting\u2026</code> Opening the WebSocket for the first time <code>connected</code> Byte relay is live <code>reconnecting \u2014 retry in Ns</code> Unexpected close; counting down to the next attempt <code>ended</code> Clean close (WebSocket code 1000) \u2014 the <code>kq</code> process exited <code>error</code> Auth failure (1008) or max reconnect attempts reached <p>Unexpected disconnects trigger an exponential-backoff reconnect loop (up to 8 attempts, 1s \u2192 15s) with inline \"Reconnecting in Ns\u2026\" messages in the terminal. A clean close or auth failure stops the loop.</p>"},{"location":"web-ui/#auth-token-gate","title":"Auth token gate","text":"<p>To restrict who can spawn a <code>kq</code> process, set <code>PTY_AUTH_TOKEN</code> on the server:</p> <pre><code>docker run -p 3000:3000 \\\n  -e KUBE_Q_URL=https://kube-q.example.com \\\n  -e PTY_AUTH_TOKEN=$(openssl rand -hex 32) \\\n  ghcr.io/mskazemi/kube_q:latest\n</code></pre> <p>Clients must supply the matching token via the <code>?token=</code> query parameter on the WebSocket URL. The frontend has a \ud83d\udd11 Token toolbar button that prompts for the token and stores it in <code>sessionStorage</code> (per-tab, cleared when the tab closes). A wrong or missing token is rejected with WebSocket close code <code>1008</code>, and the terminal shows an \"Authentication failed\" message with the token prompt re-opened.</p> <p>Leave <code>PTY_AUTH_TOKEN</code> unset for local development \u2014 the server logs <code>auth: disabled</code> and all connections are accepted.</p>"},{"location":"web-ui/#per-connection-isolation","title":"Per-connection isolation","text":"<p>Each WebSocket connection spawns a separate <code>kq</code> process in its own PTY. Sessions are completely isolated \u2014 one browser tab cannot see another's session. Closing the tab kills the <code>kq</code> process.</p>"}]}